const URl = "https://localhost:9090/buy"

const form = document.querySelector("#createVenda")
const dataVend = document.getElementById("dataVend")
const clientID = document.getElementById("clientID")
const produtoID = document.getElementById("prodID")

form.addEventListener("submit", (e) => {
    e.preventDefault()

    async function createVenda() {

        const vendData = {
            
        }



        if(!dataVend | !clientID | !produtoID){
    
        }
    }
})